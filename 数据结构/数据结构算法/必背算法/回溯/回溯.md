---
number headings: auto, first-level 1, max 6, 1.1
---
#数据结构算法 #应试笔记与八股 

# 1 目录

```toc
```

# 2 回溯概述

回溯是用于<span style="background:#fff88f"><font color="#c00000">暴力遍历</font></span>的一个方法，其：
- 相较于 `for` 循环，<font color="#c00000">其更适合树形任务而非分层任务</font>

例如：
- 组合问题：
	- 回溯分支：本次选择哪个元素
- 子集问题：
	- 回溯分支：选择或不选择某个元素
- 切割问题：
- 排列问题：
- 棋盘问题：
	- N皇后
	- 解数独

对于回溯，其<font color="#c00000">通常均可以抽象为N叉树</font>，并且：
- 叶子节点通常是遍历结果
- 非叶子节点通常是问题子集

其模板通常为：

```CPP
void backtracking(参数) {
	if(终止条件) {
		收集结果();
		return;
	}
	
	// 分层遍历 (子集问题则不需要分层，可见后续章节)
	for(遍历集合元素) {
		// 执行操作
		do_sth();
		// 执行递归
		backtracking(子节点);
		// 撤销操作
		undo_sth();
	}
}
```

具体可结合下方实例进行理解。

# 3 回溯实例

## 3.1 全排列问题

[[../../LeetCode/LeetCode题目汇总#46 全排列|LeetCode 46]]：
![[../../LeetCode/LeetCode题目汇总#46 全排列]]

回看上方回溯模板，不妨考虑如下的回溯：

```mermaid
%%{init: {
  "flowchart": {
    "curve": "linear"
  },
  "themeCSS": "
    .flowchart-link,
    .edgePath path,
    .edgePaths path,
    marker path,
    .marker,
    .edgeLabel,
    .edgeLabel rect {
      filter: none !important;
      text-shadow: none !important;
      box-shadow: none !important;
    }
  "
}}%%


flowchart LR

    subgraph L0["第 0 层：初始状态"]
        direction TB
        A["开始<br/>剩余：1, 2, 3<br/>输出：[]"]
    end

    subgraph L1["第 1 层：第一次选择"]
        direction TB
        B1["选 1<br/>剩余：2, 3<br/>输出：[1]"]
        B2["选 2<br/>剩余：1, 3<br/>输出：[2]"]
        B3["选 3<br/>剩余：1, 2<br/>输出：[3]"]
    end

    subgraph L2["第 2 层：第二次选择"]
        direction TB

        C11["选 2<br/>剩余：3<br/>输出：[1, 2]"]
        C12["选 3<br/>剩余：2<br/>输出：[1, 3]"]

        C21["选 1<br/>剩余：3<br/>输出：[2, 1]"]
        C22["选 3<br/>剩余：1<br/>输出：[2, 3]"]

        C31["选 1<br/>剩余：2<br/>输出：[3, 1]"]
        C32["选 2<br/>剩余：1<br/>输出：[3, 2]"]
    end

    subgraph L3["第 3 层：递归终止"]
        direction TB

        D111["选 3<br/>得到：[1, 2, 3]"]
        D121["选 2<br/>得到：[1, 3, 2]"]

        D211["选 3<br/>得到：[2, 1, 3]"]
        D221["选 1<br/>得到：[2, 3, 1]"]

        D311["选 2<br/>得到：[3, 1, 2]"]
        D321["选 1<br/>得到：[3, 2, 1]"]
    end

    %% 进入第 1 层
    A -->|选择| B1
    A -->|选择| B2
    A -->|选择| B3

    %% 第 1 层进入第 2 层
    B1 -->|选择 2| C11
    B1 -->|选择 3| C12

    B2 -->|选择 1| C21
    B2 -->|选择 3| C22

    B3 -->|选择 1| C31
    B3 -->|选择 2| C32

    %% 第 2 层回溯到第 1 层
    C11 -.->|撤销选择| B1
    C12 -.->|撤销选择| B1

    C21 -.->|撤销选择| B2
    C22 -.->|撤销选择| B2

    C31 -.->|撤销选择| B3
    C32 -.->|撤销选择| B3

    %% 第 2 层进入终止层
    C11 -->|选择 3| D111
    C12 -->|选择 2| D121

    C21 -->|选择 3| D211
    C22 -->|选择 1| D221

    C31 -->|选择 2| D311
    C32 -->|选择 1| D321

    %% 终止后返回第 2 层
    D111 -.->|记录并返回| C11
    D121 -.->|记录并返回| C12

    D211 -.->|记录并返回| C21
    D221 -.->|记录并返回| C22

    D311 -.->|记录并返回| C31
    D321 -.->|记录并返回| C32

    %% 各层背景
    style L0 fill:#E3F2FD,stroke:#1976D2,stroke-width:2px
    style L1 fill:#FFF3E0,stroke:#F57C00,stroke-width:2px
    style L2 fill:#F3E5F5,stroke:#7B1FA2,stroke-width:2px
    style L3 fill:#E8F5E9,stroke:#388E3C,stroke-width:2px

    %% 节点样式
    classDef root fill:#BBDEFB,stroke:#1565C0,stroke-width:2px
    classDef level1 fill:#FFE0B2,stroke:#EF6C00
    classDef level2 fill:#E1BEE7,stroke:#6A1B9A
    classDef result fill:#C8E6C9,stroke:#2E7D32,stroke-width:2px

    class A root
    class B1,B2,B3 level1
    class C11,C12,C21,C22,C31,C32 level2
    class D111,D121,D211,D221,D311,D321 result
```


```CPP
/**
 * @brief: 递归选择排列
 * @param:
 *     - result: 最终的结果列表
 *     - curr_set: 当前递归分支的剩余未选择元素
 *     - output: 当前递归分支的已选择列表
 */
void backtracking(vector<vector<int>>& result, vector<int>& curr_set, vector<int> &output) {
    if (curr_set.empty()) {
        // 当所有元素均被选择时，把选择结果加入最终结果列表
        result.push_back(output);
        return;
    }

    for (int i = 0; i < curr_set.size(); i++) {
        // 做选择
        int value = curr_set[i];
        // 将选出的元素从待选择列表中移除
        curr_set.erase(curr_set.begin() + i);
        // 加入到已选择列表
        output.push_back(value);

        // 递归
        backtracking(result, curr_set, output);

        // 撤销选择，注意两个列表均需要完全还原
        output.pop_back();
        curr_set.insert(curr_set.begin() + i, value);
    }
}

vector<vector<int>> solution(vector<int>& nums) {
    vector<vector<int>> result;
    vector<int> output;
    backtracking(result, nums, output);
    return result;
}
```

当然，上述两个数组可以合并，程序也可以继续化简。

## 3.2 子集问题 ^390chd

[[数据结构/数据结构算法/LeetCode/LeetCode题目汇总#78 子集|LeetCode 78]]：
![[数据结构/数据结构算法/LeetCode/LeetCode题目汇总#78 子集|LeetCode 78]]


```CPP
class Solution {
public:
    void backtracking(vector<vector<int>>& result, const vector<int>& nums, vector<int>& output, int curr_pos) {
        if(curr_pos == nums.size()) {
            result.push_back(output);
            return;
        }

        // 添加当前元素
        output.push_back(nums[curr_pos]);

        // 执行递归
        backtracking(result, nums, output, curr_pos + 1);

        // 删除当前元素
        output.pop_back();

        // 执行递归
        backtracking(result, nums, output, curr_pos + 1);
    }

    vector<vector<int>> subsets(vector<int>& nums) {
        vector<vector<int>> result;
        vector<int> output;
        backtracking(result, nums, output, 0);
        return result;
    }
};
```

虽然还是推荐使用更简单的[[数据结构/数据结构算法/必背算法/技巧&MISC/迭代法解子集问题#^3k7iww|迭代法解子集问题]]。



