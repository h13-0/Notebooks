---
number headings: auto, first-level 1, max 6, 1.1
---
#数据结构算法 #应试笔记与八股 

# 1 目录

```toc
```

# 2 二叉树基础

[[数据结构/数据结构基础知识/树/第五章 树与二叉树#^m1hprv|二叉树的遍历]]：
![[数据结构/数据结构基础知识/树/第五章 树与二叉树#5 3 1 二叉树的遍历 m1hprv]]

# 3 二叉树对称(镜像)的判定

二叉树对称的判定可以将其转化为如下的条件：
1. 互为镜像的两个根节点值相同
2. 互为镜像的两个根节点的子树满足镜像要求：
	![[../../../Resources/Pasted image 20260120164855.png]]

<font color="#c00000">实现时难点主要在于</font>：
- 简洁地检查左右子树为空的情况：
	1. 先判是否均为空->对称
	2. 再判是否存在空->不对称
	3. 随后即可进行值判定
- <span style="background:#fff88f"><font color="#c00000">如何同步判定条件2</font></span>：
	1. 先实现左右指针的镜像移动
	2. 递归实现时，<font color="#c00000">判别函数同时接收左侧和右侧的同步节点即可</font>
	3. 迭代实现时，

## 3.1 对称二叉树

[[../../LeetCode/LeetCode题目汇总#101 对称二叉树|LeetCode 101]]：
![[../../LeetCode/LeetCode题目汇总#101 对称二叉树]]

递归实现：

```CPP
class Solution {
public:
    // 检测来自于左侧和右侧的镜像节点对应的子树是否镜像
    bool check(TreeNode* left, TreeNode* right) {
        // 当左右均为空时返回 true
        if(left == nullptr && right == nullptr) return true;
        // 当左右节点只有一个为空时返回 false
        if(left == nullptr || right == nullptr) return false;
        // 随后左右节点必定均不为空，判值相等即可
        if(left->val != right->val) return false;
        // 递归判定左右子树
        return check(left->left, right->right) && check(left->right, right->left);
    }
	
    bool isSymmetric(TreeNode* root) {
        return check(root->left, root->right);
    }
};
```

迭代实现：

```CPP
class Solution {
public:
    bool isSymmetric(TreeNode* root) {
        deque<TreeNode*> left_deque = { root->left };
        deque<TreeNode*> right_deque = { root->right };

        while(!left_deque.empty() && !right_deque.empty()) {
            // 取队头
            auto left = left_deque.front();
            auto right = right_deque.front();
            left_deque.pop_front();
            right_deque.pop_front();
			
            // 1. 判节点是否均空
            if(left == nullptr && right == nullptr) continue;
            // 2. 判节点是否含空
            if(left == nullptr || right == nullptr) return false;
            // 3. 此时节点均非空，进行值判定
            if(left->val != right->val) return false;
            // 4. 按镜像顺序，分别将子节点加入队列进行迭代
            left_deque.push_back(left->left);
            left_deque.push_back(left->right);
            right_deque.push_back(right->right);
            right_deque.push_back(right->left);
        }
        return true;
    }
};
```

# 4 二叉树的构造

正如基础课程中所述，只要我们拿到了二叉树的<span style="background:#fff88f"><font color="#c00000">中序遍历</font></span>，<font color="#c00000">以及一个其他顺序的遍历</font>，即可构造出完整的二叉树。

其构造顺序为：
1. 找到当前子树的根节点
2. 利用中序遍历确定左右子树的范围
3. 递归构造左右子树

以[[数据结构/数据结构算法/LeetCode/LeetCode题目汇总#105 从前序与中序遍历序列构造二叉树|LeetCode 105]]为例：
![[数据结构/数据结构算法/LeetCode/LeetCode题目汇总#105 从前序与中序遍历序列构造二叉树]]




