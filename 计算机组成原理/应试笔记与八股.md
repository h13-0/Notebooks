---
number headings: auto, first-level 1, max 6, 1.1
---
#计算机组成原理 #应试笔记与八股 

# 1 目录

```toc
```

# 2 讲一下缓存一致性协议 ^o4unhf

考查企业：
- 卓驭
- 理想汽车 系统软件开发

1. 缓存一致性协议是指
2. 
3. 在早期的Intel CPU中使用的是MESI协议，现代的x86和Arm使用的则是MESI协议的变种，即x86使用的是MESIF协议、Arm使用的是MOESI协议，其增加的两个状态分别为：
	1. `Forward` 状态：
	2. `Owned` 状态：
		- 表示该缓存行持有的是最新的、已被修改的数据，而内存中的数据则是过时的
		- 允许其他缓存拥有该行的<font color="#c00000">只读</font>副本，即
4. 
5. 现代x86和Arm CPU中则使用的是MOESI协议
6. 

