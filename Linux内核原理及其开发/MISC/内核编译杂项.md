---
number headings: auto, first-level 1, max 6, 1.1
---

# 1 目录

```toc
```

# 2 内核头文件包制作与安装

内核头文件包指的是通过 `sudo apt install linux-headers-$(uname -r)` 得到的 `/lib/modules/$(shell uname -r)/build` 目录下包含头文件及Makefile等文件的头文件包。

其制作通常需要如下几个步骤：
1. 准备构建外部模块所需的头文件和生成文件：
```Shell
make prepare modules_prepare
```
2. 准备一个

