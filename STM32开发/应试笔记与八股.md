---
number headings: auto, first-level 1, max 6, 1.1
---
#嵌入式 #应试笔记与八股 

# 1 目录

```toc
```

# 2 时钟系统

## 2.1 讲一下STM32的时钟系统

1. 以STM32F1系列为例，其系统时钟有两个时钟源，分别为内部时钟源和外部时钟源。而少数其他系列产品可能还有MSI等内置时钟源。STM32F1的内部时钟源频率为8MHz，外部时钟源的频率取决于外接的晶振频率，通常也为8Mhz。
2. 在选定了系统时钟源之后，可以通过RCC设置分频器或PLL锁相环进行分频和倍频，调节为合适的系统时钟频率。
3. 而STM32F1的两个外设时钟源APB1和APB2是基于系统时钟进行分频得到的，分别对应APB1和APB2的外设频率。
4. 除了上述系统时钟系统源以外，STM32还有RTC时钟，STM32的RTC时钟也有两个时钟源，其内部集成的时钟源频率为40KHz，外部时钟源取决于具体的RTC晶振频率，往往为32.768KHz。

## 2.2 在STM32启动过程中，系统时钟树初始化之前，系统时钟是怎么样的

1. 通常STM32至少有外部时钟 `HSE` 和内部时钟 `HSI` 两个时钟源
2. 系统时钟通常指 `SYSCLK` ，此外还有 `AHB` 、`APB` 等外设时钟系统
3. 在时钟树初始化之前，默认时钟源为 `HSI` ，且 `PLL` 默认关闭，没有分频和倍频
4. 因此在系统初始化之前，通常 `SYSCLK` 、`AHB` 、`APB` 等均为 `HSI` 对应的频率

# 3 外设接口

## 3.1 ADC

### 3.1.1 讲一下STM32的ADC采样如何配置和使用

1. ADC的总体配置步骤为：
	1. 配置全局的系统时钟
	2. 使用RCC开启ADC接口
	3. 开启ADC对应的通道、采样时间
	4. 配置ADC的运行参数(例如数据分辨率、数据对其方式、外部触发源、是否连续转换等)
	5. 进行ADC校准
	6. 配置输入通道对应的GPIO，应当配置为无上下拉模式的模拟输入模式。若需要开启多通道ADC，则可以使用或运算一次配置多个GPIO。
	7. 此时就已经完成了普通的轮询ADC配置
2. 但是STM32的ADC一般结合DMA进行使用，其需要在上述步骤的基础上完成如下的配置：
	8. 启用ADC接口所对应的DMA通道的时钟
	9. 开启ADC的连续转换功能
	10. 为DMA分配一片内存，用于存储ADC的数据
	11. 配置DMA为从内存到外设，配置DMA的内存地址自动增加，将ADC外设数据源寄存器和上述内存地址填入配置结构体，初始化DMA。
	12. 配置ADC，使其在<span style="background:#fff88f"><font color="#c00000">每一个通道</font></span>的转换完成后发送DMA数据传输请求

### 3.1.2 讲一下STM32的ADC校准通常都在什么时候进行，校准时都做了什么

1. STM32的ADC校准主要是用于减小因内部电容器组的变化而造成的准精度误差。
2. STM32的ADC校准会做如下的工作：
	1. 重置校准寄存器，清除之前的校准结果
	2. 为每一个电容器计算出一个误差修正码
	3. 误差修正码会被存储到寄存器中的校准码中
3. 在实际应用中，STM32的校准通常在如下情况下进行：
	1. 系统上电初期
	2. 环境温度发生变化时
	3. 在高精度测量场景可能需要频繁校准

## 3.2 Flash

### 3.2.1 讲一下STM32读写Flash的一般步骤

1. Flash读取时，直接进行读取即可，其已完成内存空间的映射
2. 写入Flash时，需要注意：
	1. 写入前后需要对Flash进行解锁、上锁操作
	2. 通常Flash写入只能以页或者半页为单位进行擦除或写入
	3. 写Flash时尽可能不要进中断

# 4 启动相关

## 4.1 讲一讲STM32的启动流程

相关问题：
- STM32启动后C语言运行环境的建立
- 讲一讲STM32的Reset_Handler做了什么
- 讲一讲STM32的SystemInit做了什么

见专题：[[OTA、Bootloader/STM32启动流程|STM32启动流程]]。
关键步骤：
1. Cortex-M上电后，<font color="#c00000">CPU会自动</font>：
	1. 从向量表基地址(通常由 `VTOR` 指定)注入装入[[ARM内核/应试笔记与八股#^9mgydb|主堆栈指针]]
	2. 从向量表基地址的下一条地址装入程序计数器PC(通常是 `Reset_Handler`)
	3. CPU自动执行PC指向的指令
2. `Reset_Handler` 函数	![[STM32开发/OTA、Bootloader/STM32启动流程#^gb5pov]]
3. `SystemInit` 函数![[STM32开发/OTA、Bootloader/STM32启动流程#^3hxcfi]]

## 4.2 STM32的Bootloader在加载固件之前需要作什么 ^h1z73e

相关问题：
- [[嵌入式基础/应试笔记与八股#^ip561s|Bootloader在加载APP之前通常都需要做什么]]

可见章节[[STM32开发/OTA、Bootloader/STM32 OTA、Bootloader开发#^l8gh6b|STM32 Bootloader功能需求]]：
![[STM32开发/OTA、Bootloader/STM32 OTA、Bootloader开发#^l8gh6b]]

本质上来讲是重置环境、执行 `Reset_Handler` 跳转。

## 4.3 如何极致的优化Bootloader的尺寸

考查企业：
- 影石 嵌入式(简历项目相关)

优化Bootloader通常有如下的方法：
1. 将HAL库替换为LL库、设置编译器的体积优化等
2. <font color="#c00000">编译选项中关闭或调小C语言标准库的支持</font>。若关闭C语言标准库支持，则通常需要自行实现 `memcpy` 、`memset` 函数(自行实现也就是一个循环而已)。
3. <font color="#c00000">精简中断向量表</font>，只保留不可屏蔽中断(可带来上百Byte的空间)：
	- 随后外设IO失去中断功能，改为轮询方式即可。
	- 将不可屏蔽中断的地址指向
	- <font color="#c00000">并且写Flash时也最好不要进中断</font>
4. <font color="#c00000">重新实现LL库中的部分IO初始化函数</font>，改用LL的各种分项初始化和设置API，以减少复杂且不必要的逻辑判断(参数校验、超时等)
	- 此类函数包含但不限于：
		- `LL_UART_Init` 等外设初始化，可删除复杂逻辑判断
		- `SystemInit` 底层初始化，可删除：
			- 时钟初始化(改用默认时钟)
			- Flash延迟设置(默认低速时钟下可能不需要)
			- 时钟基准(改用忙等或计数器)
		- `SystemClock_Config` 等初始化API

## 4.4 讲一讲全局变量是在何时被初始化的

考察企业：
- 拓竹 嵌入式

1. 全局变量的值定义于 `.data` 段，其在 `Reset_Handler` 中被从固件拷贝到内存区域




