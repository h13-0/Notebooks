---
number headings: auto, first-level 1, max 6, 1.1
---
#自动控制 

# 1 目录

```toc
```

# 2 基础概念

## 2.1 机械臂结构定义

一个机械臂的典型结构如下：
	![[chrome_47fy5HBFP9.png]]
对于上述机械臂结构而言，其有如下的定义：
- Link：描述机械臂的一个部件
- Joint：描述机械臂的一个节点，且通常会在该节点建立一个坐标系

## 2.2 几何与坐标系定义

### 2.2.1 几何定义

上述结构的几何定义：
- Link length：转轴之间的公垂线线段长度。对于 `Link i` 的公垂线线段长度记作 $a_i$ ，需要注意下图这种非平行转轴的情况
	![[chrome_uw332YFNVA.png]]
- Link twist：公垂线和z轴的夹角。对于 `Joint i` 和 `Link i` 之间的夹角记作 $\alpha_i$ 。
	![[chrome_4tNgRZ8g63.png]]
- Link offset：两个部件(Link)之间公垂线与中间z轴交点的距离。记作 $d_i$ 。
	![[chrome_j5iOxZ215p.png]]
- Joint angle：。记作 $\theta_i$ 。
	![[chrome_Ln6BEloGOV.png]]
在大多数机械臂结构中单个节点只会变动上述四个参数中的一个，且通常变动 $\theta$ ，其次变动 $a$ 。

### 2.2.2 坐标系定义

目前机械臂坐标系定义通常采用DH(Denavit-Hartenberg)法，而DH法又分为如下两种方法：
- 标准型DH(SDH)：
	- 
- 改进型DH(MDH)：
	- 

#### 2.2.2.1 MDH坐标系定义

在几何状态定义完毕后，应当按照如下规则建立坐标系：
1. z轴方向被定义为<font color="#c00000">Joint转动或移动的方向</font>，正负通常自行确定，一般指向同一方向。
2. x轴方向被定义为 `Link length` 的方向，正向指向下一节点。最后节点的正向可自行定义或与倒数第二节点保持一致。
	- 特性：
		- 这样x轴必定与下一个节点的z轴垂直
3. y轴方向使用右手定则确定。
	![[chrome_rctazmhewp.png]]



# 3 运动学正算与建模

运动学正算是指根据各节点的状态计算末端的位置姿态信息。
在本章节的运动学正算和下一章节的运动学反算中，均使用上述章节中的坐标系定义。

## 3.1 位置矢量

位置矢量被定义为如下的矢量表示：
$$
\alpha=[x, y, z]^T
$$
位置矢量本质是用于表述相对于某个坐标系的位置关系，其可以被用于定义：
- 坐标点的点位，例如：
	![[msedge_8OclGOh97O.png]]
	则上图P点坐标可以表示为：$$P_A=[P_x, P_y, P_z]^T$$
- 坐标系的基坐标，例如世界坐标系即为 $[x, y, z]=E$ 中的 $x$ 、 $y$ 、 $z$ 均为单位位置矢量

## 3.2 旋转矩阵

可见笔记[[旋转的表述]]：![[旋转的表述]]

## 3.3 旋转位移变换的表述

### 3.3.1 对向量进行连续旋转位移

记位置矢量 $\alpha$ 经过旋转矩阵 $R$ 和平移矢量 $t$ 变换后的矢量为 $\alpha^{'}$ ，则 $\alpha^{'}$ 的普通表述(非齐次表述)形式为：
$$
\alpha^{'}=R\cdot \alpha +t
$$
其对应的齐次表述形式为：
$$
\begin{bmatrix}
\alpha^{'}\\
1
\end{bmatrix}=\begin{bmatrix}
R & t \\
0^{T} & 1
\end{bmatrix}\cdot \begin{bmatrix}
\alpha \\
1
\end{bmatrix}
$$
通常将：
$$
\begin{bmatrix}
R & t \\
0^{T} & 1
\end{bmatrix}
$$
记作旋转平移变换矩阵 $T$ ，即：
$$
\begin{bmatrix}
\alpha^{'}\\
1
\end{bmatrix}=T\cdot \begin{bmatrix}
\alpha \\
1
\end{bmatrix}
$$
则对向量进行连续旋转位移的表达式为：
$$
\alpha^{'}=T_n\cdot T_{n-1}\cdots T_1 \cdot \begin{bmatrix}
\alpha \\
1
\end{bmatrix}
$$
即连续左乘，<font color="#c00000">与外旋的表示一致</font>。需要注意，该旋转位移矩阵的定义为<span style="background:#fff88f"><font color="#c00000">先转动再移动</font></span>。

### 3.3.2 坐标系之间的连续旋转位移变换关系

现在我们考虑坐标点 $A$ 在最终坐标系下的坐标点为 $^A_nP$ ，且最终坐标系是由世界坐标系进行连续n次旋转位移变换 $T_i, i\in[1, n]$ 后得到的坐标系，现在试求解坐标点 $A$ 在直接坐标系下的坐标 $^A_0P$ 。

需要注意，<span style="background:#fff88f"><font color="#c00000">每一次的坐标系变换都是在上一个坐标系的参考下进行变换</font></span>，即第 $i$ 次变换的角度、方向是参考坐标系 $i-1$ 而非世界坐标系的，因此其为内旋。
则有经过连续n次旋转位移变换后，其基坐标系的表达为：
$$
[x_n, y_n, z_n]=T_1\cdot T_2\cdots T_n\cdot E
$$
则根据等式：
$$
[x_0, y_0, z_0]\cdot^A_nP=E\cdot^A_nP=T_1\cdot T_2\cdots T_n\cdot ^A_0P=[x_n, y_n, z_n]\cdot ^A_0P
$$
即：
$$
^A_nP=T_1\cdot T_2\cdots T_n\cdot ^A_0P
$$

## 3.4 运动学正算

### 3.4.1 MDH坐标系正算

在前文中已经给出如下的几何定义：
- $a_i$ ：
- $\alpha_i$ ：
- $d_i$ ：
- $\theta_i$ ：







